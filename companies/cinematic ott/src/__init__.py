"""Cinematic OTT — AI cinematic grading engine (Scene Reconstruction).

Modules communicate ONLY through the Look Schema JSON. The analyzer never
touches pixels; the executor never calls an AI. See files/CLAUDE.md.
"""
