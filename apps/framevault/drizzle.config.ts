import type { Config } from "drizzle-kit";

export default {
  schema: "./src/db/schema.ts",
  out: "./drizzle",
  dialect: "sqlite",
  dbCredentials: {
    url: process.env.FRAMEVAULT_DB_PATH ?? "./framevault.sqlite",
  },
} satisfies Config;
